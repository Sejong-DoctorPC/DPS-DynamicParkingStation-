import copy

markers = [0]*36
markers_copy = markers.copy()
#markers[6] = 1
if markers == markers_copy:
    print("same")
else :
    print("different")
# sector = {key:0 for key in range(1, 37)}
# print(sector)